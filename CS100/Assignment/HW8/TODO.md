# CS100 HW8 TODO

## 提交范围

- [ ] 在 `attachment/` 内完成作业代码。
- [ ] 提交前在 `attachment/` 下运行 `bash submit.sh`，生成 `submission.zip`。
- [ ] 确认提交包只包含 `src/`、`include/`、`CMakeLists.txt` 等要求文件。

## 环境状态

- [x] 已用 pixi 配置好环境。
- [x] 已在 `attachment/pixi.toml` 为 Linux 环境加入 `libgl-devel`，解决 CMake 查找 OpenGL 失败问题。
- [x] 已在 `attachment/pixi.toml` 为 Windows 环境加入 `freeglut` 依赖；按要求未重新生成环境、未构建验证。
- [x] 已验证 `pixi run cmake -B build -S .` 可完成配置。
- [x] 已验证 `pixi run cmake --build build` 可成功生成 `build/bin/PvZ`。

## 基础功能（50%）

- [x] 实现 `GameObject` 基类和 `GameWorld` 对象容器。
- [x] 实现基础界面：背景、阳光数量、至少一种僵尸卡牌、推进红线、关卡进度、红线右侧投放区域、每行脑子。
- [x] 支持点击僵尸卡牌并进入选中状态。
- [x] 支持在某一行右侧投放普通僵尸。
- [x] 投放普通僵尸时正确消耗阳光；阳光不足时禁止投放。
- [x] 投放后实现卡牌冷却或等价限制。
- [x] 在场地中生成植物防线。
- [x] 阶段推进时重新随机生成植物。
- [x] 保证同一格不重复生成植物，并设置合理贴图、动画和 HP。
- [x] 实现僵尸从右向左移动。
- [x] 僵尸碰到植物后停止并啃食植物。
- [x] 植物受伤后 HP 降低，HP 归零后消失。
- [x] 植物消失后僵尸继续移动。
- [x] 实现豌豆射手攻击同行右侧僵尸。
- [x] 豌豆射手按合理速率发射豌豆。
- [x] 豌豆命中同行第一个僵尸后造成伤害并消失。
- [x] 僵尸 HP 归零后死亡并从游戏中清理。
- [x] 实现吃掉向日葵后掉落可点击阳光。
- [x] 实现阳光循环与阳光收集。
- [x] 实现吃完所有脑子后红线右移并进入新阶段。
- [x] 实现最终阶段全吃完脑子后的胜利判定。
- [x] 实现失败判定：没有可用/可收集阳光、没有僵尸、但仍有脑子。

## 扩展功能（30%）

### 第一组：三选一

- [ ] 实现坚果墙，并随血量变化贴图/动画。
- [ ] 或实现铁桶僵尸，并随血量变化贴图/动画。
- [ ] 或实现路障僵尸，并随血量变化贴图/动画。

### 第二组：二选一

- [ ] 实现撑杆跳僵尸及其阶段行为/动画。
- [ ] 或实现蹦极僵尸及其阶段行为/动画。

## 代码规范与工程结构

- [ ] 自己编写的代码编译无 warning。
- [ ] 使用智能指针和标准库容器管理对象。
- [ ] 至少新增一个 `.hpp` 和一个 `.cpp`。
- [ ] 按类别分目录，并为每个新增目录建立 `CMakeLists.txt`。
- [ ] 在上级 `CMakeLists.txt` 中使用 `add_subdirectory` 引入新增目录。
- [ ] 相似类逻辑用基类和虚函数统一，避免复制粘贴。
- [ ] 正确使用 `enum class` 和 `utils.hpp` 中已有枚举，避免 magic numbers。
- [ ] 成员变量尽量保持 `private`。
- [ ] 不通过 `dynamic_cast`、`typeid` 或 `ImageID` 判断具体类型；改用虚函数或类别信息。
- [ ] 合理使用 `static`、`const`、`virtual`。
- [ ] 使用合理继承关系。
- [ ] 构造函数使用初始化列表。
- [ ] 非必要不使用显式类型转换或 C 风格类型转换。
- [ ] 变量尽量靠近使用位置声明。
- [ ] 避免不必要的全局变量。
- [ ] 为每个函数写注释；长函数内按模块写细分注释。

## 建议实现顺序

- [x] 先确认项目能编译运行。
- [x] 实现 `GameObject` 基类和 `GameWorld` 对象容器。
- [x] 添加背景。
- [x] 添加投放区域，并支持点击生成普通僵尸。
- [x] 添加脑子和植物。
- [x] 实现僵尸啃植物和吃脑子。
- [x] 添加阳光、卡牌和冷却。
- [x] 添加豌豆射手和豌豆碰撞。
- [x] 添加阶段推进与胜负判定。
- [x] 最后实现扩展功能。
